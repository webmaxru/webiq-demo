# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: webiq-demo
metadata:
  template: webiq-demo@0.1.0

# Provision-only azd. `azd provision` creates the Free-tier Static Web App, the
# scale-to-zero Container Apps backend, and monitoring. GitHub Actions deploys the
# frontend artifact and the public ghcr.io backend image.
