# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: webiq-demo
metadata:
  template: webiq-demo@0.1.0

# Single combined container: the Express server serves both the REST API
# (/api/*) and the built React SPA on one origin. Cheapest topology — one
# Container App that defaults to a single warm replica (minReplicas: 1, billed
# at the idle rate); set WEBIQ_MIN_REPLICAS=0 to scale to zero instead.
services:
  app:
    project: .
    language: ts
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
      # Build the image on ACR (ACR Tasks) instead of a local Docker daemon —
      # enables headless provision/deploy without Docker Desktop running.
      remoteBuild: true
